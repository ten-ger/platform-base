# Platform Base

Platform Base is ...

## Getting Started

To start a new project, clone this repo to a new directory:

```bash
npm start
```

To build the app for production, run:

```bash
npm run build
```
